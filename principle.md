# 算法原理与实现机制 (Algorithm Principles)

本文档旨在解释 WoW Stat Solver 背后的数学模型以及代码的具体实现逻辑。

## 1. 理论模型 (Theoretical Model)

魔兽世界的伤害/治疗计算模型本质上是一个**乘法模型**。我们的目标是在总绿字预算（Budget）有限的情况下，合理分配四种副属性（爆击 $C$、急速 $H$、精通 $M$、全能 $V$），以最大化最终收益。

### 1.1 目标函数
总收益 $Score$ 可以表示为各属性收益系数的乘积：

$$ Score = f_c(C) \times f_h(H) \times f_m(M) \times f_v(V) $$ 

其中：
*   $C, H, M, V$ 为投入的绿字点数。
*   $f_x(x)$ 为该属性对应的收益函数（通常由 SimC 模拟数据拟合得到的多项式曲线）。
*   约束条件：$C + H + M + V \le Budget$ 

### 1.2 最优解条件：等边际收益原理
根据**拉格朗日乘数法**，当函数取得极值时，各变量方向上的**相对边际收益（Relative Marginal Gain）**应当相等。

通俗地说，假设我们手里有最后 1 点属性预算：
*   如果投给爆击能提升 0.5% 总伤害，投给急速能提升 0.6% 总伤害。
*   那么显然应该投给急速。
*   随着急速越来越高，边际收益递减，直到投给急速和投给爆击的收益一致时，即达到平衡（最优解）。

数学上，我们需要比较的是**对数导数**：
$$ \frac{f'_c(C)}{f_c(C)} = \frac{f'_h(H)}{f_h(H)} = \frac{f'_m(M)}{f_m(M)} = \frac{f'_v(V)}{f_v(V)} $$ 

---

## 2. 工程实现 (Implementation)

在 `solver.js` 中，我们并没有通过求解上述方程组来计算结果，而是采用了一种更稳健、更符合计算机逻辑的**贪婪算法 (Greedy Algorithm)**。

### 2.1 为什么不求解析解？
1.  **分段函数的复杂性**：SimC 的收益曲线是分段拟合的（Piecewise Polynomials），且包含了暴雪官方的递减（DR）断点。这些断点导致函数在某些位置**不可导**或导数突变，直接求解方程组极其困难且容易陷入局部陷阱。
2.  **离散性**：游戏内的属性点数是整数，而非连续实数。

### 2.2 贪婪算法逻辑
我们需要模拟“一步步加点”的过程：

1.  **初始化**：将所有属性重置为 0（或锁定值）。
2.  **迭代寻找最优步**：
    *   设定一个步长（Step，例如 100 点）。
    *   分别计算如果把这 100 点投入到 C、H、M、V 中，谁带来的**相对提升**最大。
    *   **相对提升判据**：
        $$ Gain = \frac{Multiplier_{next}}{Multiplier_{current}} $$ 
    *   将这 100 点真正投入到收益最高的那个属性中。
3.  **动态步长**：为了平衡精度与性能，算法采用“从粗到细”的策略。
    *   先用大步长（如 100）快速填满大部分预算。
    *   再用小步长（如 5、1）进行微调，确保最终结果精确。

### 2.3 核心代码解读
代码中并没有计算导数 $f'(x)$，而是计算了**有限差分商**。

```javascript
// 伪代码逻辑
const currentMult = getMultiplier(stat, currentVal);
const nextMult    = getMultiplier(stat, currentVal + step);
const gain        = nextMult / currentMult; // 计算相对收益率
```

**这种实现的优势：**
*   **规避奇点**：即使拟合曲线在两段交界处稍微不平滑（导数不连续），直接比较 $f(x+\Delta)$ 和 $f(x)$ 的值也能保证找到确实提升更高的一方。
// ... existing code ...
*   **自适应 DR**：由于 `getMultiplier` 内部已经集成了 DR 惩罚逻辑，贪婪算法会自动避开收益骤降的深层递减区间，无需额外编写逻辑。

## 3. 数学证明与算法正确性 (Mathematical Proof)

本节从数学角度证明：为什么看似简单的“分步贪心”策略能够收敛到全局最优解。

### 3.1 隐藏前提：状态函数的无后效性 (Path Independence)
我们的目标函数 $D(C,H,M,V)$ 本质上是一个**状态函数 (State Function)**。
*   **定义**：最终的总收益只取决于最终的四维坐标 $(c,h,m,v)$，而与到达该坐标的“路径”无关。
*   **推论**：这意味着 $\oint \nabla D \cdot d\mathbf{r} = 0$。无论算法是“先加满 C 再加 H”，还是“C 和 H 交替加”，只要最终资源分配总量一致，结果就是一致的。
*   **算法意义**：这赋予了贪心算法“犯错”的权利。算法不需要在每一步都走出完美的直线（对角线），只要它最终能到达那个山顶，中间走一些折线（Zig-Zag）也是合法的。

### 3.2 自动纠偏机制：凸性与交叉协变 (Convexity & Synergy)
贪心算法之所以不会陷入局部最优，是因为数学模型本身具有强大的**自动纠偏**能力。这种能力源于目标函数的二阶导数性质：

1.  **自身负反馈 (Self-Correcting)**：
    $$ \frac{\partial^2 D}{\partial C^2} < 0 $$
    由于收益递减（Diminishing Returns），一旦算法在某一步“错误”地给 C 分配了过多资源，C 的边际收益率（导数）会迅速下降，从而在下一步降低其优先级。

2.  **交叉正反馈 (Synergy)**：
    $$ \frac{\partial^2 D}{\partial C \partial H} > 0 $$
    由于伤害公式是乘法 ($C \times H \times \dots$)，增加 C 会**物理上提高** H 的收益率。

**纠偏过程**：如果您在一个 Step 中“贪心”地全投给了 C，上述两个机制会立刻生效：C 变贵了，H 变香了。在下一轮循环中，梯度的方向会自动旋转，**强迫**算法掉头去投资 H。

### 3.3 离散梯度上升 (Discrete Gradient Ascent)
关于“为什么不需要在 Step 内部寻找最优混合方案”：
*   该算法本质上是一个**离散梯度上升**过程。
*   虽然在单个 Step 内部，算法选择了“当前最陡峭的一个方向”进行曼哈顿移动（Manhattan Move），而非混合移动。
*   但由于上述的“自动纠偏”机制，随着 Step 的迭代和缩小（代码中从 100 缩小到 1），这条锯齿状的折线路径会**无限逼近**理想的梯度流曲线，最终收敛到全局最优解。


```
